(ns ransom-note.config)


(def CHUNK_SIZE 1024) ;; Define the chunk size constant

;;(def SMALL_MAGAZINE_FILE_SIZE_MB (* 4 1024 1024))  ;; 4 MB in bytes

(def MAX_MESSAGE_FILE_SIZE_MB (* 1024))  ;; 1 KB in bytes
